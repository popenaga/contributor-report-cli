# Changesets

Add a markdown file in this directory for every user-facing change that should affect the next npm release.

Typical flow:

1. Run `npx changeset`
2. Choose the bump type
3. Describe the change
4. Commit the generated file with your code changes
